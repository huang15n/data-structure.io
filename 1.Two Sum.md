
1. Two Sum
Easy


Given an array of integers nums and an integer target, return indices of the two numbers such that they add up to target.

You may assume that each input would have exactly one solution, and you may not use the same element twice.

You can return the answer in any order.

Example 1:

Input: nums = [2,7,11,15], target = 9
Output: [0,1]
Explanation: Because nums[0] + nums[1] == 9, we return [0, 1].
Example 2:

Input: nums = [3,2,4], target = 6
Output: [1,2]
Example 3:

Input: nums = [3,3], target = 6
Output: [0,1]

Constraints:

2 <= nums.length <= 104
-109 <= nums[i] <= 109
-109 <= target <= 109
Only one valid answer exists.


analysis: 

loop through the entire array, use the target to subtract each element. Form a dictionary and store the elements inside the dictionary 
for instance, if element A being subtract and left the result, store the result inside the dictionary value 
search through each time and validate if the element is inside the dictionary value or not 


one edge case is that the check condition has to be at the front 


```python
"""
Runtime: 108 ms, faster than 48.16% of Python3 online submissions for Two Sum.
Memory Usage: 15.7 MB, less than 9.66% of Python3 online submissions for Two Sum.
"""
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        dictionary_result = {}
        for index, item in enumerate(nums):
            if item in dictionary_result.keys():
                return [dictionary_result[item],index]
            value = target - item
            dictionary_result[value] = index
            print(value)
            
            
```


```java

/*
Runtime: 4 ms, faster than 68.48% of Java online submissions for Two Sum.
Memory Usage: 45.6 MB, less than 35.18% of Java online submissions for Two Sum.
*/


class Solution {
    public int[] twoSum(int[] nums, int target) {
      
        Map<Integer, Integer> mapResult = new HashMap<>();
        for(int i = 0; i < nums.length; i++){
            if(mapResult.containsKey(nums[i])){
                return new int[]{mapResult.get(nums[i]),i};
            }
        
            int result = target - nums[i];
            mapResult.put(result, i);
            
            
            
        }
        
        return new int[]{};
        
        
        
        
    }
        
     
}

```